#pragma once

#include <cstdint>
#include <string>

// Simplified structure for spell IDs
struct SpellInfo {
    uint32_t ID = 0;                       // Field 1
};

// REMOVED: constexpr size_t SPELL_DBC_ROW_SIZE = 680;

// REMOVED: ReadFromBuffer template functions

// REMOVED: FetchLocalizedRowFn typedef and addresses 